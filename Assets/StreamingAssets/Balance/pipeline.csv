# Pipeline — system run order (ARCHITECTURE §4.2)
#
# Order is data. Reordering a system, or disabling one to isolate a bug, is an edit to
# this file and a relaunch — never a recompile.
#
# The loader fails loudly on a system declared here that no code implements, and on any
# implemented system missing from this file. Silent omission is the failure mode being
# prevented: a system that quietly never runs is close to undebuggable.
#
# Orders must be unique within a phase. Two systems claiming the same slot would leave the
# run order ambiguous, and ambiguous order is non-determinism (§7.1).
#
# Leave gaps of 10 so a system can be inserted without renumbering its neighbours.
#
# Where an ordering is load-bearing design rather than arbitrary, say so here. The known
# one, for when these systems exist: Wages must precede Unrest, so an unpaid wage feeds
# grievance the same day rather than the next (GDD §5.2.3).
#
# Order here is the order in ARCHITECTURE §4.2, and it is load-bearing:
#
#   Consumption before Production  — the crew eat yesterday's stock, not today's output. That
#                                    one-day lag is what makes reserves matter.
#   Wages before Upkeep            — people are paid before buildings are maintained.
#   Wages before Unrest            — an unpaid wage must feed grievance the same day rather
#                                    than the next (GDD §5.2.3). Unrest arrives in Phase 2;
#                                    leave room before it.
#   Market after Production        — today's output is sold today, but the coin lands after
#                                    today's wages were paid. Income always funds tomorrow,
#                                    which is what makes reserves the real resource.
#   Desertion after Wages,         — people leave because of what happened to them today, and
#   before Production                what is left of the crew is what works today. This is the
#                                    one irreversible step in the day: everything else recovers
#                                    on its own, so without it shocks add instead of compound.
#
# CommandDrain is not here yet: nothing constructs a dispatcher until there is something to
# command. When it lands, its position decides when input takes effect relative to all of
# this, which is a design decision and belongs in this file.

phase,order,system,enabled
DayBoundary,10,Consumption,true
DayBoundary,20,Wages,true
DayBoundary,30,Upkeep,true
DayBoundary,35,Desertion,true
DayBoundary,40,Production,true
DayBoundary,50,Market,true
