# Log channel thresholds. Edit and relaunch; no rebuild.
#
# A channel logs at its level and everything more severe. Off silences it entirely.
# Levels: Trace, Debug, Info, Warn, Error, Off
#
#   Trace   firehose: per-entity, per-tick. Off unless something is being hunted.
#   Debug   how the machinery ran: what loaded, what was skipped, timings.
#   Info    milestones worth seeing while scanning the file.
#   Warn    wrong but survivable: a default substituted, a file missing.
#   Error   broken.
#
# The '*' row is the threshold for every channel, applied before the rows below, so a channel
# omitted here still gets a sensible level.
#
# Channel names are the LogChannel enum, so a typo is a load failure naming the valid ones —
# never a phantom channel configured while the real one silently keeps its default. Same rule
# for level names. A channel that quietly stops logging is the logging equivalent of a system
# missing from pipeline.csv.
#
# None of this is the kill switch. Log.Enabled turns everything off in one boolean read, for
# when logging itself is the performance problem.

# These levels are the FILE's. The Unity console has its own floor, set in code at Warn, so
# turning a channel up here fills the log without filling the console. The two answer different
# questions: the file is the record of what the engine did, the console is for noticing that
# something is wrong while the editor happens to be open. A console carrying every day boundary
# is a console nobody reads, and a real warning scrolls past unseen.
#
# Pipeline and Commands ship at Debug because that is the in-depth view worth having by default:
# every system as it runs, in declared order, and every command as it is queued, applied or
# refused, with the DAG node id that its events point back at. It is a few dozen lines a day,
# which is nothing against a twenty-minute day, and it is the difference between reading what
# the engine did and guessing.
#
# Trace stays off everywhere. It is the per-entity firehose, and turning it on is something you
# do while hunting one specific thing.

channel,level
*,Info
Boot,Info
Content,Info
Pipeline,Debug
Commands,Debug
Events,Info
State,Info
Game,Info
Misc,Info
